# Core server + storage
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
websockets==14.1

# Numerics / calibration
numpy==1.26.4
scikit-learn==1.5.2

# Perception (off-the-shelf glue — heavy; installed lazily, see README)
# torch CPU build is pulled in by ultralytics
ultralytics==8.3.58
opencv-python==4.10.0.84
fast-plate-ocr==0.3.0
lap==0.5.13            # ByteTrack linear assignment
torchreid==0.2.5       # OSNet ReID backbone
gdown==6.1.0           # torchreid weight downloads
tensorboard==2.21.0    # torchreid import-time dependency (unused here)

# Evaluation
matplotlib==3.11.0

# 3D vehicle model bridge (local project, installed editable):
#   pip install -e D:\comp_vison_projs\Car_Gen_and_Modeling_proj
# cargen provides single-photo 3DGS reconstruction + gated fusion; see car3d/.

# Testing
pytest==8.3.4
httpx==0.28.1
